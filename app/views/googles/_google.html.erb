latlng<%= google.id %> = new google.maps.LatLng(<%= google.latlng %>);

//var contentString<%= google.id %> = '<%= to_table_tag(google.content) %>';

var infowindow<%= google.id %> = new google.maps.InfoWindow({
	content: <%= to_table_tag(google.content) %>
});

var marker<%= google.id %> = new google.maps.Marker({
	position: latlng<%= google.id %>,
	map: map,
	title: 'Makrer <%= google.id %>'
});

google.maps.event.addListener(marker<%= google.id %>, 'click', function() {
	infowindow<%= google.id %>.open(map,marker<%= google.id %>);
});